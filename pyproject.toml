[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-research"
version = "0.1.0"
description = "A modular agentic research system"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
